<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <receiver
            android:name=".WingWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="app.wingpact.android.WIDGET_WATER_PLUS" />
                <action android:name="app.wingpact.android.WIDGET_WATER_MINUS" />
                <action android:name="app.wingpact.android.WIDGET_SOS" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/wing_widget_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- CAMERA is for the <input type="file" capture="environment"> meal-photo
         capture flow. READ_MEDIA_IMAGES/READ_EXTERNAL_STORAGE/
         WRITE_EXTERNAL_STORAGE were removed 2026-09-09: the app never reads
         or writes the shared media store directly (no @capacitor/camera or
         @capacitor/filesystem plugin is installed) — every photo comes
         through a plain WebView <input type="file">, which on Android
         launches the system photo picker via an intent and returns a
         content:// URI the app can read without holding any storage
         permission itself. Declaring these anyway is exactly what triggers
         Google Play's "Photo and Video Permissions" policy — which requires
         a completed Permissions Declaration Form justifying the access —
         and got this app flagged/enforced (Sep 8, 2026). Since the app has
         no real use for them, removing them is the actual fix, not a form
         to fill out. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Health Connect: only steps are used. Remove all other permissions the
         @capgo/capacitor-health plugin declares but we do not need. -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_STEPS" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_DISTANCE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_DISTANCE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_ACTIVE_CALORIES_BURNED" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_HEART_RATE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_WEIGHT" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_WEIGHT" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_SLEEP" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_SLEEP" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_RESPIRATORY_RATE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_RESPIRATORY_RATE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_OXYGEN_SATURATION" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_OXYGEN_SATURATION" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_RESTING_HEART_RATE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_RESTING_HEART_RATE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_HEART_RATE_VARIABILITY" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_HEART_RATE_VARIABILITY" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_VO2_MAX" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_VO2_MAX" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_BLOOD_PRESSURE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_BLOOD_PRESSURE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_BLOOD_GLUCOSE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_BLOOD_GLUCOSE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_BODY_TEMPERATURE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_BODY_TEMPERATURE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_HEIGHT" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_HEIGHT" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_FLOORS_CLIMBED" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_FLOORS_CLIMBED" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_BODY_FAT" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_BODY_FAT" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_BASAL_BODY_TEMPERATURE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_BASAL_BODY_TEMPERATURE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_BASAL_METABOLIC_RATE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_BASAL_METABOLIC_RATE" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_TOTAL_CALORIES_BURNED" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_MINDFULNESS" />
    <uses-permission tools:node="remove" android:name="android.permission.health.WRITE_MINDFULNESS" />
    <uses-permission tools:node="remove" android:name="android.permission.health.READ_EXERCISE" />
</manifest>
